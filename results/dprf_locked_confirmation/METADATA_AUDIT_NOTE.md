# 元数据状态字段说明

本次运行在执行前已由 `LOCKED_CONFIRMATION_PROTOCOL.md` 和
`locked_config.json` 冻结，配置中的 `analysis_status` 为
`locked_internal_confirmation`。

通用执行器的顶层 `exploratory` 字段仍固定显示为 `true`，这是继承的元数据措辞，
不代表本次运行进行了确认标签驱动的调参。指标、混淆矩阵、模型参数、训练设置和
评价数值均未在运行后修改。
